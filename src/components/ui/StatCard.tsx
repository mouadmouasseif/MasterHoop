import React from 'react';
import { cn } from '@/src/lib/utils';
export default function StatCard({ icon, value, label, color = 'text-white', trend }: { icon: React.ReactNode, value: string, label: string, color?: string, trend?: string }) { return <div className="glass-card p-5 flex flex-col items-center justify-center text-center relative overflow-hidden group"><div className={cn('mb-3 p-3 rounded-full bg-white/5 transition-transform group-hover:scale-110', color)}>{React.cloneElement(icon as React.ReactElement, { size: 24 } as any)}</div><div className="text-2xl font-bold italic serif tracking-tight">{value}</div><div className="text-[10px] text-white/40 uppercase font-mono mt-1 tracking-widest">{label}</div>{trend && <div className={cn('absolute top-3 right-3 text-[8px] font-bold px-1.5 py-0.5 rounded', trend.includes('+') || (trend.includes('%') && !trend.includes('-')) ? 'bg-brand-neon/20 text-brand-neon' : 'bg-red-500/20 text-red-400')}>{trend}</div>}</div>; }
