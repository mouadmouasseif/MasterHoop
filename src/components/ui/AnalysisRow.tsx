import { cn } from '@/src/lib/utils';
export default function AnalysisRow({ label, value, status, warning = false, active = false }: { label: string, value: string, status: string, warning?: boolean, active?: boolean }) { return <div className="flex items-center justify-between group"><div className="space-y-1"><div className="text-white/40 text-xs uppercase font-mono">{label}</div><div className="text-lg font-bold">{value}</div></div><div className={cn('px-3 py-1 rounded-lg text-[10px] font-bold uppercase transition-all duration-300', warning ? 'bg-red-500/10 text-red-400' : active ? 'bg-brand-orange text-white neon-orange-shadow scale-110' : 'bg-brand-neon/10 text-brand-neon')}>{status}</div></div>; }
